import PropTypes from "prop-types";

const AdminStationDetail = ({stationId}) => {
    console.log(stationId);
  return (
    <div>AdminStationDetail</div>
  )
}
AdminStationDetail.propTypes = {
    stationId: PropTypes.object
}

export default AdminStationDetail

